"use strict";

function testFunction(parameter){
    if (parameter){
       if (function returnInputAsOutput(input){
        return input === input;
       ) return true;
     else {
        return false;
    }
}